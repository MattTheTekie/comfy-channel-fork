server {
# put desired port below
    listen 8080;
    server_name _;

    location / {
# put path to www below
            root /www;
            index index.html;
        }

    # Serve static files if they exist
    location /media {
        root /dev/shm/hls;
    }
}