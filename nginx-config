server {
# put desired port below
    listen 8080;
    server_name _;

    location / {
            root /www;
            index index.html index.htm;
        }

    # Serve static files if they exist
    location /media {
        root /dev/shm/hls;
    }
}